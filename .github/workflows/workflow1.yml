name: simple flow

on: [push]

jobs:
  run-shell-command:

    runs-on: deere-ubuntu-latest
    

    steps:
      - name: echo a string
        run: echo "Hello world"

      - name: multiline script
        
        run: |
          node -v
          npm -v
